create table insert_outbound(
	id int NOT NULL auto_increment,
	年 int NOT NULL,
	月 int NOT NULL,
	國家名稱 varchar(15) NOT NULL,
	年齡 varchar(15) NOT NULL,
	性別 varchar(15) NOT NULL,
	交通方式 varchar(15) NOT NULL,
	primary key(id)
);
